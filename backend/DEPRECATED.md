# DEPRECATED — Backend FastAPI

**Fecha de deprecación:** 2026-05-31

**Motivo:** El backend fue migrado a Hono (TypeScript) en el directorio `backend-node/`.  
Este directorio contiene el código legacy en FastAPI (Python) y ya no es el backend activo del proyecto.

**Backend activo:** [`../backend-node/`](../backend-node/) — Hono 4.7 + TypeScript 5.8 + Zod + Supabase JS

**No usar este directorio para nuevo desarrollo.** Todo el nuevo código de backend debe ir en `backend-node/`.

**Nota:** El código en este directorio se preserva como referencia histórica. Será eliminado en una limpieza futura del repositorio.
